# 🔄 Server Running on Port 3001

## ✅ Port Changed

Server ถูกเปลี่ยนจาก port 3000 เป็น **port 3001** แล้ว

---

## 🌐 New Endpoints

### Health Check
```
http://localhost:3001/health
```

### Swagger Documentation
```
http://localhost:3001/api/docs
```

### API Base URL
```
http://localhost:3001
```

---

## 📋 Configuration

ไฟล์ `.env` ถูกอัปเดตแล้ว:
```
PORT=3001
```

---

## 🔍 ตรวจสอบว่า Server รันแล้ว

### วิธีที่ 1: เปิด Browser
ไปที่:
```
http://localhost:3001/health
```

**ควรเห็น:**
```json
{
  "status": "ok",
  "timestamp": "...",
  "uptime": ...
}
```

### วิธีที่ 2: ตรวจสอบ Port
```powershell
netstat -ano | findstr :3001 | findstr LISTENING
```

**ควรเห็น:** มี process กำลัง LISTENING ที่ port 3001

---

## 🛑 หยุด Server

```powershell
Get-Process -Name node | Stop-Process -Force
```

---

## 🔄 เปลี่ยน Port กลับเป็น 3000

แก้ไขไฟล์ `.env`:
```
PORT=3000
```

แล้วรัน server ใหม่

---

**Status:** Running on Port 3001

